{
  "name": "settlesure",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Razorpay-style payment gateway settlement reconciliation",
  "scripts": {
    "reconcile": "tsx src/cli.ts",
    "generate": "tsx src/cli.ts --generate-only",
    "test": "vitest run",
    "dashboard": "npm run dev --prefix dashboard",
    "check-baseline": "node scripts/check-baseline.mjs",
    "sweep-decoys": "tsx scripts/sweep-decoys.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
